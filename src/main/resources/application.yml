
server:
  port: 8080
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id:
            client-secret:
            scope: repo
  profiles:
    active: develop
